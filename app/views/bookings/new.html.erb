<div class="container">
  <%= render "form_booking", superpower: @superpower, booking: @booking %>

  <div class="form-footer">
    <%= link_to(superpower_path(@superpower)) do %>
      <i class="fa-solid fa-circle-left"></i> Back
    <% end %>
  </div>
</div>
