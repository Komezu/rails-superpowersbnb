<div class="container">
  <div class="form-container">
    <h3>Modify this superpower</h3>

    <%= simple_form_for(@superpower, url: update_superpower_path(@superpower)) do |f| %>
      <%= f.input :name %>
      <%= f.input :description %>
      <%= f.input :photo, as: :file %>
      <%= f.input :category, as: :select, collection: Superpower::CATEGORIES.map { |c| [c.capitalize, c] } %>
      <%= f.input :price_per_day %>
      <%= f.submit class: "btn btn-warning" %>
    <% end %>
  </div>

  <div class="form-footer">
    <%= link_to(my_superpowers_path) do %>
      <i class="fa-solid fa-circle-left"></i> Back
    <% end %>
  </div>
</div>
